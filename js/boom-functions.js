jQuery(document).ready(function ($) {
  //console.log('jQuery ' + $().jquery + ' is firing.');

  //toggle the menu on cellphones
  // $('.primary-nav h2 a').click(function() {
  //   $('.primary-nav .menu-block-wrapper').toggleClass('is-expanded');
  //   return false;
  // });

  // //my version of doubletaptogo
  // $('.primary-nav .expanded > a').click(function(event) {
  //   if (!$(this).hasClass('armed') && !$(this).parent().hasClass('active-trail') && $('html').hasClass('touch')) {
  //     event.preventDefault();
  //     $(this).addClass('armed');
  //   }
  // });

});